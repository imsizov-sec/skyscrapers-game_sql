# 🏙️ Skyscrapers (Игра «Небоскрёбы») на PL/SQL

Полнофункциональная реализация логической головоломки «Небоскрёбы» (разновидность латинского квадрата с условиями видимости), работающая полностью внутри базы данных **Oracle**.

Вся логика, отрисовка поля (ASCII-графика) и управление состоянием реализованы на языке **PL/SQL**. Взаимодействие с игроком происходит через вызов хранимых процедур и вывод в `DBMS_OUTPUT`.

## 🎯 Правила игры

Необходимо заполнить сетку `N x N` зданиями разной высоты (от 1 до N) так, чтобы:
1.  **Латинский квадрат:** В каждой строке и каждом столбце высоты не повторялись.
2.  **Видимость:** Числа по краям поля показывают, сколько зданий видно с этой точки (более высокие здания загораживают низкие).

## 🚀 Ключевые возможности

* **Разные размерности:** Поддержка полей 4x4 (Easy), 5x5 (Medium), 6x6 (Hard), 7x7 (Expert).
* **Режимы игры:**
    * *Каталог:* Выбор конкретной головоломки по Seed.
    * *Случайная:* Генерация игры по уровню сложности.
    * *Игра дня:* Ежедневный челлендж, единый для всех игроков.
* **Игровой процесс:**
    * Установка значений и «карандашных» пометок.
    * Система **Undo / Redo** (отмена и возврат хода).
    * Валидация состояния поля в реальном времени.
* **Социальные функции:**
    * Глобальная таблица лидеров (Leaderboard).
    * Рейтинг «Игры дня» (Daily Board) с учетом времени решения.
* **Импорт/Экспорт:** Возможность сохранить текущее состояние игры в строку и загрузить её на другом клиенте.
* **История игр:** Полный реплей (воспроизведение) всех ходов завершенных партий.

## 🛠 Технологический стек

* **СУБД:** Oracle Database 12c+
* **Язык:** PL/SQL
* **Инструментарий:** Oracle SQL Developer / DBeaver / PL/SQL Developer (любой клиент с поддержкой `DBMS_OUTPUT`).
* **Архитектура:**
    * Хранение состояния в реляционных таблицах (`GAME_SESSIONS`, `GAME_STEPS`).
    * Пакет `SKYSCRAPERS` — API для игрока.
    * Пакет `SKYSCRAPERS_UTILS` — ядро логики и отрисовки.
    * `DBMS_SCHEDULER` — для генерации ежедневных заданий и очистки сессий.

## 🎮 Как играть (Интерфейс)

Взаимодействие осуществляется через SQL-команды. Перед началом включите вывод `DBMS_OUTPUT`.

### 1. Главное меню
```sql
BEGIN
  SKYSCRAPERS.START_MENU;
END;
